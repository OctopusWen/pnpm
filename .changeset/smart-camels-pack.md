---
"pacquet": patch
"pnpm": patch
---

`pnpm pack` and `pnpm publish` now include bundled workspace dependencies when using the isolated linker [pnpm/pnpm#1643](https://github.com/pnpm/pnpm/issues/1643).
